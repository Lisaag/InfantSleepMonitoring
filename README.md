This project is an experimental project to determine sleep states of infants in the NICU, using video data. To determine the sleep state, only video data is used.

The project consists of three parts
1. Eye localization
2. REM classification
3. Sleep state classification

For each of the parts there is a separate subdirectory. Each subdirectory contains a README with more information about the corresponding part.